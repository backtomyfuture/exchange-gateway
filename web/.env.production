# Application title (shown in browser tab and loading screen)
VITE_TITLE=Exchange Gateway

# Public path (must start and end with /)
VITE_PUBLIC_PATH = '/'

# API base path
VITE_BASE_API = '/api/v1'

# Enable compression
VITE_USE_COMPRESS = true

# Compression type
VITE_COMPRESS_TYPE = gzip